export enum CacheStrategy {
  None,
  Memory,
  LocalStorage,
}

export enum ListenerAction {
  ADD = 'ADD',
  REMOVE = 'REMOVE',
}

export enum ReqMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}
